import React from "react";

const Typography = ({ children }: { children: React.ReactNode }) => {
  return <span className="text-[#4a5568] text-xs my-2">{children}</span>;
};

export default Typography;
